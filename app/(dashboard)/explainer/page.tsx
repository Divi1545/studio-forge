import { ModuleStub } from "@/components/module-stub";

export default function ExplainerPage() {
  return (
    <ModuleStub
      title="Explainer Studio"
      phase={8}
      description="Script to storyboard to assembled explainer video with narration."
    />
  );
}
