import { ModuleStub } from "@/components/module-stub";

export default function CinemaPage() {
  return (
    <ModuleStub
      title="Cinema Studio"
      phase={7}
      description="The timeline: scene list, preview player, drag-to-reorder tracks, and render queue."
    />
  );
}
