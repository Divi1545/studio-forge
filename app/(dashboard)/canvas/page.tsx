import { ModuleStub } from "@/components/module-stub";

export default function CanvasPage() {
  return (
    <ModuleStub
      title="Canvas"
      phase={9}
      description="Visual workspace connecting assets and scenes as a node graph."
    />
  );
}
