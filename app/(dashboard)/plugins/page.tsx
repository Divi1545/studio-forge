import { ModuleStub } from "@/components/module-stub";

export default function PluginsPage() {
  return (
    <ModuleStub
      title="Plugins"
      phase={9}
      description="Routing table editor: view and edit provider mappings, enable/disable providers, manage API keys."
    />
  );
}
