import { ModuleStub } from "@/components/module-stub";

export default function VideoPage() {
  return (
    <ModuleStub
      title="Video Studio"
      phase={4}
      description="Image-to-video, text-to-video, lip sync, camera motion presets, and scene continuation."
    />
  );
}
