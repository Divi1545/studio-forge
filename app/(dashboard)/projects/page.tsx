import { ModuleStub } from "@/components/module-stub";

export default function ProjectsPage() {
  return (
    <ModuleStub
      title="Projects"
      phase={1}
      description="Project dashboards with characters, locations, props, scripts, audio, video, and timeline tabs."
    />
  );
}
