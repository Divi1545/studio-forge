import { ModuleStub } from "@/components/module-stub";

export default function AssetsPage() {
  return (
    <ModuleStub
      title="Assets"
      phase={1}
      description="The universal asset browser: folders, grid, filters, search, and version history."
    />
  );
}
