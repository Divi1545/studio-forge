import { ModuleStub } from "@/components/module-stub";

export default function MarketingPage() {
  return (
    <ModuleStub
      title="Marketing Studio"
      phase={8}
      description="Product URL or brief in, ad creative out: scripts, hooks, voiceover, video, and caption packs."
    />
  );
}
