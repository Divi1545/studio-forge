import { ModuleStub } from "@/components/module-stub";

export default function DirectorPage() {
  return (
    <ModuleStub
      title="AI Director"
      phase={6}
      description="Brief in, plan out: the agentic AI Director plans, gets approval, then executes a full production run."
    />
  );
}
