import { ModuleStub } from "@/components/module-stub";

export default function AudioPage() {
  return (
    <ModuleStub
      title="Audio Studio"
      phase={5}
      description="TTS, voice cloning, dialogue, sound effects, music, and podcast generation."
    />
  );
}
