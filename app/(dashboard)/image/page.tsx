import { ModuleStub } from "@/components/module-stub";

export default function ImagePage() {
  return (
    <ModuleStub
      title="Image Studio"
      phase={3}
      description="Character, Location, Prop, Creature, Storyboard, and Freeform builders with the prompt template compiler."
    />
  );
}
